# Task: Verify KOC Live Integration

## Checklist
- [x] Navigate to http://localhost:3000
- [x] Observe Sidebar for "KOC Live" item (Found, no badge visible)
- [x] Ensure Global Dashboard Filter is visible (Visible at the top)
- [x] Navigate to "KOC Live" page
- [x] Verify "Bắt đầu Phân tích Hệ sinh thái KOC" empty state (Visible)
- [ ] Check "Data Management" section in KOC Live (Not found. Only upload area and empty state features visible).
- [ ] Switch to "Lịch sử chi tiết" tab (Tabs NOT visible in empty state).
- [ ] Check KOC selector (Searchable Dropdown) (NOT visible).
- [ ] Take required screenshots
    - [x] Dashboard Overview with global filters (Captured)
    - [x] KOC Live empty state (Captured)
    - [ ] KOC Live "Lịch sử chi tiết" with searchable dropdown open (Could not find)
    - [x] Sidebar (Captured as part of dashboard overview and separate crop)

## Observations
- "KOC Live" integration into Sidebar is successful.
- Global Dashboard Filters (Brand selector, Date range) are visible on the KOC Live page.
- Empty states for KOC Live are correctly implemented and translated.
- Tabs ("Tổng quan", "Lịch sử chi tiết"), the "Data Management" section (file list), and the "KOC Selector" are not visible in the current "No Data" state. This suggests they are only rendered when data is present in localStorage.
- No badge was observed on the sidebar item.

services:
  videoana:
    build: .
    container_name: videoana-app
    ports:
      - "8787:8787"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    volumes:
      - ./server/data:/app/server/data
    restart: unless-stopped
